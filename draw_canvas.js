/* Draws the kanban board and articulates all its elements and objects. */
// Kanban object and subobjects
// the board will need multiple divisions
// events--need elements like title, time, description
// needs to be interactive, and each interaction should communicate a change to
// the server
